pipeline {
   agent any

   stages {
          stage('build') {
            steps {
              echo 'installing required libraries...'
              sh 'pip install -r requirements.txt'
         }
         }

      stage('test') {
        steps{      
          echo 'running tests...'
          sh 'python3 test_TRN.py'
        }
        }

  }
}
