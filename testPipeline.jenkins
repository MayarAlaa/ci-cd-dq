pipeline {
   agent any

   stages {
          stage('build') {
            steps {
              sh 'pip install exceptiongroup'
         }
         }

      stage('runpy') {
        steps{      
          sh 'python3 test_TRN.py'
        }
        }

  }
}
